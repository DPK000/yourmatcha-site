# Client-side (Vite) — public values, ingebakken in bundle
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_STRIPE_MODE=test
# VITE_STRIPE_PUBLIC_KEY=  # Niet nodig voor Stripe Checkout (hosted) — alleen voor Elements
